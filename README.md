# OpenGLCellularAutomataCpp

For project compiling, if you want to try out the demo (ImplementationOpenGL.cpp), you'll
need to add the following libs to ExternalLibs
  - GLEW
  - GLFW
  - ImGUI
  - std_image
  - glm
